# UPS 智能选型助手 — 产品开发文档

> 版本：v1.7.1
> 最后更新：2026-05-25
> 本文档定位：产品功能、技术架构、计算公式

---

## 一、产品概述

### 1.1 背景与定位

UPS 智能选型助手是一款**无需安装的单文件本地 Web 工具**，面向公司内部销售和技术人员使用。

用户痛点：销售/技术人员收到客户 UPS 技术规格书后，需要手动打开 Excel 逐条比对，过程费时易出错。

本工具将流程简化为：
```
粘贴客户需求文档  →  AI 解析参数并匹配型号  →  一键导出推荐方案
```

### 1.2 核心特性

| 特性 | 说明 |
|------|------|
| 零安装 | 单个 HTML 文件，双击打开 |
| 离线数据 | 内置 179 款 UPS 产品 |
| AI 驱动 | 支持 OpenAI/DeepSeek/Moonshot/Claude 等 API |
| 防幻觉 | Prompt 约束 + 前端过滤双重校验 |
| 多轮对话 | 支持追问细化 |
| 电池配置 | 铅酸（方法1/2）、锂电池、开关箱/柜、监控 |
| 项目汇总 | 整合 UPS+电池+开关箱+监控的完整配置清单 |
| 价格保护 | 汇总表价格需输入密码（314159）查看 |
| 选型历史 | 保存选型结果（最多50条），支持搜索/导出/恢复 |
| 图片OCR | 粘贴技术文档截图，AI视觉识别文字 |

### 1.3 内置数据

| 数据 | 数量 | 说明 |
|------|------|------|
| 太行 UR | 21 款 | 工频机型为主 |
| 昆仑 UE | 39 款 | 三进三出，含长机/标机 |
| 祁连 UM | 22 款 | 模块化，支持热插拔 |
| 泰山 UT | 97 款 | 全系列覆盖 |
| 电池恒功率 | 62 型号 | SP12V / SPG12V高功率 / GFMD-2V / 电能GBT12-20小时率 / 电能GBT12-10小时率 |
| 电池监控 | 15 型号 | 主机/模块/传感器等 |

---

## 二、界面结构

```
┌─────────────────────────────────────────────────────┐
│  ⚡ UPS 智能选型助手                                  │
│  [导入Excel] [电池计算] [查看数据库] [⚙️设置]          │
├─────────────────────────────────────────────────────┤
│  🔋 电池配置计算面板（可折叠，三个Tab）                  │
│    [铅酸电池] [锂电池] [开关盒]                        │
├─────────────────────────────────────────────────────┤
│  📊 产品数据库（可折叠，双栏：产品信息 + 泰尔规格）       │
├─────────────────────────────────────────────────────┤
│  📋 客户需求                                          │
│    [文本框：粘贴需求]              [📎上传文档] [🔍选型] │
├─────────────────────────────────────────────────────┤
│  🤖 AI 分析（需求提取/容量分析/匹配逻辑/注意事项）       │
├─────────────────────────────────────────────────────┤
│  📦 推荐产品                          [导出推荐方案]  │
│    ★ 推荐方案（N款）  |  ○ 备选方案（N款）            │
├─────────────────────────────────────────────────────┤
│  💬 继续优化（多轮对话）                              │
└─────────────────────────────────────────────────────┘
```

---

## 三、功能详细说明

### 3.1 AI 选型系统

#### API 配置（设置 → API配置）

| 提供商 | Base URL | 默认模型 |
|--------|----------|----------|
| OpenAI | `https://api.openai.com/v1` | `gpt-4o` |
| DeepSeek | `https://api.deepseek.com/v1` | `deepseek-chat` |
| Moonshot | `https://api.moonshot.cn/v1` | `moonshot-v1-8k` |
| Anthropic | `https://api.anthropic.com/v1` | `claude-sonnet-4-6` |
| ✏️ 自定义 | 用户自填 | 用户自填 |

#### AI 提示词系统（设置 → AI提示词）

**6 个解析维度**：
1. 基本电气参数（容量、相数、电压、THDI、PF）
2. 架构与可靠性（热插拔、并机、MTBF）
3. 性能指标（效率、过载、ECO）
4. 蓄电池要求（后备时间、电池类型）
5. 通信与监控（RS485/Modbus/SNMP）
6. 认证与标准（泰尔认证、IEC）

**输出格式**：
```json
{
  "analysis": "■ 客户关键需求提取...\n■ 容量选型分析...\n■ 产品匹配逻辑...\n■ 注意事项...",
  "recommended": [{ "model": "型号", "reason": "不少于80字" }],
  "also_consider": [{ "model": "型号", "reason": "备选原因" }]
}
```

### 3.2 Excel 导入

**标准 UPS 系列处理**：
| 系列 | 基础信息 Sheet | 技术规格 Sheet |
|------|---------------|---------------|
| 太行 UR | `UR太行` | `UR泰尔` |
| 昆仑 UE | `UE昆仑` | `UE泰尔` |
| 祁连 UM | `UM祁连` | `UM泰尔` |
| 泰山 UT | `UT泰山` | `UT泰尔` |

**非标准 Sheet 自动识别**：
- 型号列：优先匹配 `型号` → `产品型号` → 含"型号"的列名
- 系列列：优先匹配 `系列` → `所属产品系列` → `类别`
- 跳过 Sheet：`UR泰尔/UE泰尔/UM泰尔/UT泰尔/电池泰尔/配置计算公式`

### 3.3 文档上传

支持格式：TXT / MD / DOCX / DOC / **PDF**

| 格式 | 实现方式 |
|------|----------|
| TXT/MD/CSV | 直接读取文本 |
| DOCX/DOC | mammoth.js（CDN） |
| PDF | PDF.js（CDN） |

### 3.4 图片粘贴与OCR识别（v1.6）

**功能说明**：用户可以粘贴技术文档截图，AI自动识别图片中的文字并填入需求框。

**实现方式**：
| 组件 | 说明 |
|------|------|
| 粘贴监听 | 全局 `paste` 事件捕获，提取 `clipboardData.items` 中的图片 |
| 图片预览 | 显示缩略图，支持删除 |
| AI识别 | 调用支持视觉的大模型（GPT-4V、Claude3、kimi2.5等） |

**使用限制**：
- 必须使用支持视觉识别的AI模型
- 图片以Base64格式存储在内存中，刷新页面后丢失
- 推荐模型：kimi2.5、Claude3、GPT-4V

**代码位置**：
- `handleImagePaste` - 全局粘贴事件处理
- `extractTextFromImages` - AI图片识别函数
- `callAI` - 支持 `images` 参数的多模态调用

### 3.5 选型历史记录（v1.6）

**功能说明**：保存每次AI选型的输入和结果，支持回溯和导出。

**数据存储**：
```javascript
localStorage["ups_selection_history"] = {
  version: 1,
  records: [{
    id: "uuid-timestamp",
    timestamp: "2026-04-01 10:30:00",
    requirement: "客户需求文本",
    analysis: "AI分析内容",
    recommended: [{model, reason}],
    also_consider: [{model, reason}],
    projectSummary: {...}  // 当时的数据快照
  }]
}
```

**限制**：最多保存50条，超出删除最早记录

**核心函数**：
| 函数 | 功能 |
|------|------|
| `saveSelectionHistory` | 保存选型结果 |
| `loadSelectionHistoryRaw` | 加载历史数据 |
| `getFilteredHistory` | 搜索过滤 |
| `restoreHistory` | 恢复历史记录 |
| `exportHistory` | 导出Excel |

**UI入口**：顶部工具栏"📋 历史"按钮

### 3.6 电池配置计算

#### 3.4.1 铅酸电池（方法一）

**输入参数**：
| 参数 | 默认值 |
|------|--------|
| UPS容量 (kVA) | 500 |
| 功率因数 | 1 |
| 电池节数 | 36 |
| 单格个数 | 6（12V电池）|
| 截止电压 (V) | 1.75 |
| 电池逆变效率 | 0.95 |
| 电池功率 (W) | 546 |

**计算公式**：
```
负载功率(W) = UPS容量 × 功率因数 × 1000
单格功率 = 负载功率 / (电池节数 × 单格个数 × 逆变效率)
电池组数 = 负载功率 / (电池节数 × 单格个数 × 逆变效率 × 电池功率)
```

#### 3.4.2 锂电池

**输入参数**：
| 参数 | 默认值 |
|------|--------|
| 负载功率 (kW) | 空（选填）|
| UPS功率 (kVA) | 600 |
| 功率因数 cosφ | 0.8 |
| 电池组数量 | 2 |
| 放电时间 (h) | 0.25 |
| 3.2V模组电芯串联数 | 160 |
| 逆变器效率μ | 0.95 |
| 电池输出效率 | 0.95 |

**计算公式**：
```
功率(W) = IF(负载功率为空, UPS功率×cosφ×1000, 负载功率×1000)
电芯平台电压 = 查表：≤1C→3.15V，1~3C→3.12V，4~6C→3.05V，>6C→3.02V
标称电压 = 电芯串联数 × 3.2V
计算电池容量 = 功率 × 放电时间 / (μ × S × N × η × U)
```

#### 3.4.3 电池开关箱/柜

**输入参数**：
| 参数 | 默认值 |
|------|--------|
| UPS容量 (kVA) | 200 |
| 功率因数 | 0.9 |
| 电池节数/组 | 30 |
| 电池组数 (N) | 1 |
| 终止电压 (V) | 1.75 |
| 电池逆变效率 | 0.95 |

**计算公式**（详见第四章）：
```
负载功率 = UPS容量 × 功率因数 × 1000（始终用UPS额定功率）
最大放电电流 I = 负载功率 / (电池节数 × 终止电压 × 6 × 逆变效率)
电压等级 = 按最大充电电压判断（≤250V→250VDC，251~500V→500VDC，>500V→750VDC）
电池总开关 = I（向上取整50A，不乘安全系数）
电池分开关 = I/(N-1)（N≥2时，向上取整50A）
```

### 3.5 电池监控配置

**组件清单**：
| 组件 | 产品编码 | 说明 |
|------|----------|------|
| 主机 | 88091156 | 每6组配1台 |
| HMI | 88091157 | 选配 |
| 单体测量模块 | 88091145/88091146 | 每只电池1个 |
| 测试线缆 | 88091147/88091148/88091149 | 根据端子类型M6/M8/M10 |
| 电压采集 | 88091150 | 选配 |
| 电流监控 | 88091138 | 每组1个 |
| 电流传感器 | 88091139-88091144 | 根据电流量程 |

---

## 四、关键计算公式汇总

### 4.1 电池开关箱/柜计算

| 计算项 | 公式 | 说明 |
|--------|------|------|
| 负载功率 | `P = upsCap × pf × 1000` | 始终用UPS额定功率 |
| 最大放电电流 | `I = P / (cells × endV × cellMultiplier × eff)` | cellMultiplier: 12V电池=6, 2V电池=1 |
| 分开关电流 | `I_sub = I / (groups - 1)` | 仅 groups≥2 时有效 |
| 电压等级 | `getVoltLevel(cells, battType)` | 基于最大充电电压 |

### 4.2 电压等级（新公式 v1.4.8）

```
最大充电电压 = 2.35V × 单格数 × 电池节数
- 2V电池：单格数=1
- 12V电池：单格数=6

电压等级判断：
≤250V → 250VDC
251~500V → 500VDC
>500V → 750VDC
```

### 4.3 传感器量程选择（重要！）

```
传感器量程基于【分开关电流】选择：
- groups ≥ 2: 传感器电流 = maxCurrent / (groups - 1)
- groups = 1: 传感器电流 = maxCurrent
```

### 4.4 电池架铜排规格（v1.5.3 修正）

```
单组电池 (groups=1): 铜排用主开关规格 (mainSwitchSize)
多组电池 (groups≥2): 铜排用分开关规格 (subSwitchSize)
```

### 4.5 关键变量说明

| 变量 | 含义 | 用途 |
|------|------|------|
| `groups` | 单台UPS电池组数 | 分开关路数、分开关电流 |
| `upsNum` | UPS台数 | 开关箱数量 |
| `totalGroups` | 总组数 = groups × upsNum | 监控主机数量 |

**业务规则**：
- 每台UPS配1个电池开关箱/柜
- 分开关路数 = groups（不是totalGroups！）
- 分开关电流 = maxCurrent / (groups - 1)，groups≥2时有效

---

## 五、数据格式

### 5.1 PRODUCTS 数组

```javascript
{
  "型号": "UE-2000TPL",
  "系列": "昆仑UE",
  "描述": "昆仑Power系列200kVA三进三出长机",
  "安装": "塔式",
  "并机数量": "4",
  "功率因数": "0.9",
  "满载效率": "0.9639",
  "输入电压范围(V)": "三相: 285~476",
  "通讯": "RS485/Modbus",
  // 泰尔技术规格字段...
}
```

### 5.2 localStorage 存储

```javascript
// API 配置
localStorage["ups_config"] = JSON.stringify({
  provider: "deepseek",
  model: "deepseek-chat",
  apikey: "sk-...",
  baseurl: "https://api.deepseek.com/v1",
  customProviderName: "硅基流动",     // 自定义提供商
  customBaseurl: "https://api.siliconflow.cn/v1"
})

// 自定义提示词
localStorage["ups_custom_prompt"] = "..."
```

---

## 六、版本历史

| 版本 | 日期 | 主要变更 |
|------|------|----------|
| v0.1 | 2026-03-04 | 初始版本：内置数据 + 基础 AI 选型 |
| v1.0 | 2026-03-04 | 专业提示词系统 + 结果展示拆分 |
| v1.2 | 2026-03-09 | 电池配置计算 + Excel导入持久化 |
| v1.3 | 2026-03-14 | 铅酸方法2 + 恒功率数据表弹窗 + 监控配置 |
| **v1.4** | **2026-03-20** | **UPS与电池联动 + 项目汇总清单 + 价格保护** |
| v1.4.8 | 2026-03-21 | 电压等级新公式 + UPS中性线 |
| v1.4.9 | 2026-03-23 | UPS型号自动填入功能 |
| v1.4.11 | 2026-03-23 | 祁连UM模块化分两行 |
| v1.4.12 | 2026-03-23 | 非标UPS支持 + 可编辑字段 |
| **v1.5** | **2026-03-24** | **电池架功能 + 电池开关选项 + 样式优化** |
| v1.5.3 | 2026-03-31 | 电能GBT12V电池 + 变量重复声明Bug修复 |
| **v1.5.4** | **2026-03-31** | **电池架铜排规格修复** |
| **v1.6** | **2026-04-01** | **选型历史记录 + 图片OCR识别 + Excel自动行高** |
| v1.6.1 | 2026-04-11 | 电池开关箱计算公式改为始终用UPS额定功率 |
| v1.6.2 | 2026-04-11 | 修正电能GBT12V恒功率数据（15个型号全部与Excel模板一致） |
| v1.6.3 | 2026-04-30 | 电池分类名规范化（SP12V/SPG12V高功率/GFMD-2V/电能GBT12-20小时率） |
| v1.6.4 | 2026-04-30 | 电池数据清洗（删除SP系列不带b的型号） |
| v1.6.5 | 2026-04-30 | 电池分类精确推荐、无结果显示计算参数、死代码清理、pre-push hook |
| v1.6.6 | 2026-05-06 | 新增电能GBT12-10小时率电池数据（8个型号GPT12-38~250）、null值显示为"-"、浮点精度清理 |
| v1.6.7 | 2026-05-22 | 优化UPS与电池配置计算中的UPS型号选择，下拉框可直接切换型号，无需先删除已选型号；同步开发入口、Netlify入口和工程脚本 |
| v1.6.8 | 2026-05-23 | 建立APP_VERSION单一版本来源，页面标题和底部版本号自动同步；新增版本一致性检查脚本，防止文档和页面版本漏改 |
| v1.6.9 | 2026-05-23 | 新增数据中心方案校核独立面板，支持场景参数、UPS容量与N/N+1/2N/分布式冗余校核、缺失参数和工程风险提示 |
| v1.6.10 | 2026-05-23 | 修正数据中心2N校核口径，UPS台数改为总台数并按A/B路拆分；导入现有UPS参数优先读取UPS与电池配置计算面板 |
| v1.6.11 | 2026-05-23 | 优化数据中心方案校核的架构与数量建模：N/N+1/分布式显示UPS并机台数，2N显式填写A/B路UPS台数并自动汇总，避免架构和UPS台数口径冲突 |
| v1.6.12 | 2026-05-23 | 优化N架构校核文案：N架构显示可用容量和UPS并机台数计算过程，不再使用故障后可用容量/故障切换负载率表述 |
| v1.6.13 | 2026-05-23 | 修正N+1架构数量口径：输入工作UPS台数N，总台数自动按N+1计算，故障后可用容量按N台UPS校核 |
| v1.6.14 | 2026-05-23 | 数据中心方案校核与UPS电池配置联动：上方UPS总台数默认同步到下方UPS台数，下方手动修改后不再自动覆盖；修正导入现有UPS参数读取字段 |
| v1.6.15 | 2026-05-23 | 优化N+1输入文案，将“工作UPS台数(N)”改为更贴近工程表达的“并机UPS数量(N)” |
| v1.6.16 | 2026-05-23 | 电池放电时间和截止电压选项改为从恒功率数据表动态生成，避免旧版硬编码漏掉可用工况 |
| v1.6.17 | 2026-05-23 | 电池类型新增自定义/客户指定模式，跳过内置电池推荐并提示在项目汇总清单中手动填写电池型号 |
| v1.7.0 | 2026-05-25 | 第二阶段新增电池后备时间反算独立面板，支持按UPS负载、电池型号、组数和截止电压反推预计后备时间并输出风险提示 |
| v1.7.1 | 2026-05-25 | 统一后备时间字段命名，修正数据中心校核、UPS与电池配置计算、电池后备时间反算之间的后备时间导入同步 |

详细Bug修复记录见 `UPS选型助手_开发说明.md`

---

*本文档由 Claude Code 自动生成*
