# UPS 智能选型助手 — 产品开发文档

> 版本：v1.2.3
> 最后更新：2026-03-10
> 技术负责：内部开发

---

## 一、产品概述

### 1.1 背景与定位

UPS 智能选型助手是一款**无需安装的单文件本地 Web 工具**，面向公司内部销售和技术人员使用。

用户痛点：销售/技术人员收到客户 UPS 技术规格书（自然语言文档）后，需要手动打开 Excel 表格逐条比对，判断哪些型号满足要求，过程费时易出错。

本工具将整个流程简化为三步：
```
粘贴客户需求文档  →  AI 解析参数并匹配型号  →  一键导出推荐方案
```

### 1.2 核心特性

| 特性 | 说明 |
|------|------|
| 零安装 | 单个 HTML 文件，双击打开，无需服务器 |
| 离线数据 | 内置 179 款产品（可随时导入新 Excel 替换） |
| 数据持久化 | 导入的 Excel 数据存入 IndexedDB，刷新不丢失 |
| AI 驱动 | 支持 OpenAI / DeepSeek / Moonshot / Claude 等任意兼容 API |
| 自费模式 | 用户填写自己的 API Key，调用费用自担 |
| 防幻觉 | 双层校验（Prompt 约束 + 前端过滤）杜绝 AI 虚构型号 |
| 多轮对话 | 支持追问细化，AI 持续记忆上下文 |
| 提示词可定制 | 内置专业提示词，支持用户修改保存 |
| 动态数据源 | 支持导入更新版 Excel，无需重新开发 |
| 电池配置计算 | 支持铅酸电池、锂电池、开关盒配置计算 |

### 1.3 用户场景

**主要场景**：销售/技术人员在电脑端使用，将客户发来的技术规格书（Word/邮件文字）直接粘贴或上传，获取选型建议。

**典型流程**：
1. 客户发来数据中心 UPS 技术规格书
2. 销售打开工具，粘贴规格书文字（或上传 DOCX）
3. AI 自动提取：容量、电压范围、相数、并机需求、认证要求等
4. 从 200+ 款产品中匹配推荐产品 + 备选产品
5. 导出 HTML 推荐方案文件，发给客户或存档

---

## 二、文件结构

```
UPS选型助手/
│
├── UPS选型助手.html              ← 核心产品文件（主程序，约 260KB）
├── 常用UPS速查表-V7.5.xlsx       ← UPS 产品数据源
├── 锂电池-选型模板-A00.xlsx     ← 锂电池选型参考模板
├── UPS选型助手_开发文档.md       ← 产品开发文档
├── UPS选型助手_开发说明.md      ← 工程化开发说明
│
├── src/                         ← 源代码目录（工程化用）
│   └── css/
│       └── style.css            ← 样式表
│
├── dev_scripts/                 ← 开发辅助脚本
│   ├── build.py                ← 构建脚本
│   ├── serve.py                ← 开发服务器
│   ├── test.py                 ← 统一测试入口
│   ├── check_prompt_feature.py  ← 验证提示词功能
│   ├── verify.py               ← 验证双表数据库视图
│   ├── check_html.py           ← HTML 基础结构检查
│   ├── scan_js.py              ← JS 代码扫描
│   ├── inspect_excel.py        ← Excel 数据结构检查
│   ├── test_import_logic.py     ← Excel 导入测试
│   └── （历史修复脚本）
│       fix_v3.py / fix_v4.py / ...
│
└── test_data/                  ← 测试数据
    └── 常用UPS速查表-V7 测试.xlsx
```

---

## 三、功能详细说明

### 3.1 界面结构

```
┌─────────────────────────────────────────────────────┐
│  ⚡ UPS 智能选型助手                                    │
│  [导入Excel] [电池计算] [查看数据库] [⚙️设置]          │
├─────────────────────────────────────────────────────┤
│  🔋 电池配置计算面板（可折叠，三个Tab）                  │
│    [铅酸电池] [锂电池] [开关盒]                        │
├─────────────────────────────────────────────────────┤
│  📊 产品数据库（可折叠）                                │
│    上栏：产品信息（型号/描述/容量/安装/并机/通讯等）     │
│    下栏：泰尔技术规格（输入/输出/电气参数）              │
├─────────────────────────────────────────────────────┤
│  📋 客户需求                                          │
│    [文本框：粘贴客户需求]              [📎上传文档]    │
│                                        [🔍 开始选型]  │
├─────────────────────────────────────────────────────┤
│  🤖 AI 分析（选型结果）                               │
│    ■ 客户关键需求提取                                 │
│    ■ 容量选型分析                                     │
│    ■ 产品匹配逻辑                                     │
│    ■ 注意事项                                         │
├─────────────────────────────────────────────────────┤
│  📦 推荐产品                          [导出推荐方案]  │
│    ★ 推荐方案（N款）                                  │
│    [表格：系列/型号/描述/容量/PF/效率/电压/安装/并机/通讯/详情] │
│    ○ 备选方案（N款）                                  │
│    [同上格式]                                         │
├─────────────────────────────────────────────────────┤
│  💬 继续优化                                          │
│    [对话历史]                                         │
│    [输入框] [发送]                                    │
└─────────────────────────────────────────────────────┘
```

### 3.2 API 配置（设置 → API配置 Tab）

支持以下 API 提供商（快速切换，自动填入 Base URL 和默认模型）：

| 提供商 | Base URL | 默认模型 |
|--------|----------|----------|
| OpenAI | `https://api.openai.com/v1` | `gpt-4o` |
| DeepSeek | `https://api.deepseek.com/v1` | `deepseek-chat` |
| Moonshot | `https://api.moonshot.cn/v1` | `moonshot-v1-8k` |
| Anthropic | `https://api.anthropic.com/v1` | `claude-sonnet-4-6` |
| ✏️ 自定义 | 用户自填名称 + Base URL | 用户自填 |

选择"自定义"后，界面展开一行：
- **自定义提供商名称**：仅用于标记（如：硅基流动、阿里云百炼）
- **Base URL**：填入任意兼容 OpenAI 接口的地址

配置保存在浏览器 `localStorage`（key: `ups_config`），刷新页面不丢失。

> **费用说明**：工具本身免费，API 调用按所选提供商计费，通常每次选型约 0.01~0.05 元（DeepSeek 最便宜）。

### 3.3 AI 提示词系统（设置 → AI提示词 Tab）

**设计思路**：将选型分析的专业逻辑写入系统提示词，让 AI 以专业 UPS 工程师视角分析需求，输出结构化结果。

**默认提示词覆盖 6 个解析维度**：

```
1. 基本电气参数
   - 额定容量（kVA/kW）、功率因数
   - 输入相数（单进单出/三进三出）
   - 输入电压范围、输入 THDI、输入 PF
   - 输出电压精度、THDU、转换时间

2. 架构与可靠性
   - 热插拔模块化需求
   - 并机台数（N+1/2N冗余）
   - 安装方式（机架/落地/塔机）
   - MTBF/MTTR 要求

3. 性能指标
   - 系统效率（%@负载率）
   - 过载能力
   - ECO节能模式

4. 蓄电池要求
   - 后备时间
   - 电池类型（铅酸/锂电）
   - 均充/浮充管理

5. 通信与监控
   - RS485/Modbus/SNMP/以太网
   - DCIM/BMS 集成

6. 认证与标准
   - 泰尔认证（YD/T 2165）
   - 抗震报告
   - IEC 62040 等
```

**输出格式强制要求**：
- `analysis` 字段：必须包含 4 个 ■ 分节（需求提取 / 容量分析 / 匹配逻辑 / 注意事项）
- `reason` 字段：不少于 80 字，逐项说明参数满足情况
- `recommended` / `also_consider`：只能使用合法型号清单中的型号

**提示词存储**：`localStorage`（key: `ups_custom_prompt`）

### 3.4 Excel 导入逻辑

支持导入任意结构的 UPS 产品 Excel，自动识别各 Sheet 类型。

#### 标准 UPS 系列处理（4个系列）

| 系列 | 基础信息 Sheet | 技术规格 Sheet |
|------|---------------|---------------|
| 太行 UR | `UR太行` | `UR泰尔` |
| 昆仑 UE | `UE昆仑` | `UE泰尔` |
| 祁连 UM | `UM祁连` | `UM泰尔` |
| 泰山 UT | `UT泰山` | `UT泰尔` |

每个系列的产品信息和技术规格通过**型号字段**进行 JOIN 合并。

#### 非标准 Sheet 自动识别规则

| 检测项 | 策略 |
|--------|------|
| 型号列 | 优先匹配 `型号` → `产品型号` → 含"型号"的列名 → `产品编码/编号` |
| 系列列 | 优先匹配 `系列` → `所属产品系列` → `类别` → 找不到则用 Sheet 名 |
| 描述列 | `描述` → `产品描述` → `备注` |
| 表头行 | 扫描前 5 行，找含"型号"关键词的行作为表头 |
| 重复表头 | `isHeaderValue()` 过滤（识别含"型号"/"参数"等关键词的数据行） |
| 跳过 | `UR泰尔 / UE泰尔 / UM泰尔 / UT泰尔 / 电池泰尔 / 配置计算公式` |

#### 导入后处理

1. 显示绿色 Toast 提示（含各系列数量）
2. 刷新产品数据库视图（双栏表格）
3. 更新页面顶部数据来源标签
4. `PRODUCTS` 全局变量替换为新数据

### 3.5 产品数据库查看器

双栏展示，对应 Excel 中的产品信息与泰尔技术规格：

| 栏位 | 颜色 | 内容 |
|------|------|------|
| 上栏（产品信息） | 蓝色表头 | 型号/系列/描述/容量/安装/并机/通讯等 |
| 下栏（技术规格） | 绿色表头 | 输入电压/频率/THDI/输出电压精度/效率/转换时间等 |

功能：
- 全文搜索（型号/描述/系列）
- 点击列头排序（数字/中文均支持）
- 上下栏行悬停联动高亮

### 3.6 文档上传

支持格式：TXT / MD / DOCX / DOC / **PDF**
实现方式：
- TXT/MD/CSV：直接读取文本
- DOCX/DOC：通过 mammoth.js（CDN）解析
- **PDF**：通过 PDF.js（CDN）解析，逐页提取文本内容

结果：文件内容自动填入需求文本框，用户可预览修改后选型

### 3.7 电池配置计算（v1.2 新增）

点击顶部"🔋 电池计算"按钮展开电池配置计算面板，包含三个Tab：

#### 3.7.1 铅酸电池配置计算

**输入参数**：
| 参数 | 说明 | 默认值 |
|------|------|--------|
| 负载功率 (kW) | 选填，如有负载功率则优先使用 | 空 |
| UPS容量 (kVA) | UPS额定容量 | 500 |
| 功率因数 | cosφ | 1 |
| 电池节数 | 电池组串联节数 | 36 |
| 单格个数 | 通常为6 | 6 |
| 截止电压 (V) | 放电终止电压 | 1.75 |
| 电池逆变效率 | 高频0.95/工频0.94，泰山UT取0.94 | 0.95 |
| 电池功率 (W) | 查电池恒功率表后填入 | 546 |

**计算逻辑**：
```
IF(负载功率为空, UPS容量 × 功率因数 × 1000, 负载功率 × 1000) → 负载功率(W)
单格功率 = 负载功率 / (电池节数 × 单格个数 × 逆变效率)
电池组数 = 负载功率 / (电池节数 × 单格个数 × 逆变效率 × 电池功率)
```

#### 3.7.2 锂电池配置计算

**输入参数**：
| 参数 | 说明 | 默认值 |
|------|------|--------|
| 负载功率 (kW) | 选填，如有则优先使用 | 500 |
| UPS功率 (kVA) | 选填，负载功率为空时使用 | 600 |
| 功率因数cosφ | 0.8~1 | 0.8 |
| 电池组数量 | 并联组数 | 2 |
| 放电时间 (h) | 后备时间 | 0.25 |
| 3.2V模组电芯串联数 | 模组内电芯串联数量 | 160 |
| 逆变器效率μ | 泰山UT取0.94 | 0.95 |
| 电池输出效率 | 锂电池放电效率 | 0.95 |

**计算逻辑**（来源：锂电池-选型模板-A00.xlsx）：
```
功率(W) = IF(负载功率为空, UPS功率×cosφ×1000, 负载功率×1000)
电池放电倍率 = 1 / 放电时间
电芯平台电压 = 查表：≤1C→3.15V，1~3C→3.12V，4~6C→3.05V，>6C→3.02V
标称电压 = 电芯串联数 × 3.2V
计算电池容量 = 功率 × 放电时间 / (μ × S × N × η × U)
```

#### 3.7.3 开关盒配置计算

**输入参数**：
| 参数 | 说明 | 默认值 |
|------|------|--------|
| UPS容量 (kVA) | UPS额定容量 | 200 |
| 功率因数 | cosφ | 0.9 |
| 电池节数/组 | 每组电池节数 | 30 |
| 终止电压 (V) | 放电截止电压 | 1.75 |
| 电池逆变效率 | 高频0.95/工频0.94 | 0.95 |

**计算逻辑**：
```
负载功率 = UPS容量 × 功率因数 × 1000
最大放电电流 = 负载功率 / (电池节数 × 终止电压 × 6 × 逆变效率)
电压等级：≤20节→250VDC，21~36节→500VDC，>36节→750VDC
推荐开关容量 = 最大电流 × 1.25（向上取整50A）
```

### 3.8 选型结果展示

**推荐产品卡片** — 分两个紧凑子区块：

```
★ 推荐方案（N款）
─────────────────────────────────────────────────────
[系列][型号][描述][容量][PF][效率][输入电压范围][安装][并机][通讯][详情▶]

○ 备选方案（N款）  ← 无备选时自动隐藏
─────────────────────────────────────────────────────
[同上格式]
```

**详情弹窗** — 点击"详情"按钮弹出完整参数：
- 产品型号 + 系列
- 产品描述
- AI 推荐理由（完整文字）
- 所有技术参数（键值对逐项展示）

### 3.8 导出推荐方案

点击"导出推荐方案"生成独立 HTML 文件（可直接打印 / 存档 / 发给客户），内容包含：
1. 标题页（项目信息、日期）
2. 客户需求摘要
3. AI 分析内容
4. 推荐产品详情（每款单独展开，含所有参数）

### 3.9 多轮对话

初次选型完成后底部出现"继续优化"对话框：
- AI 持有完整对话历史（`chatHistory` 数组）
- 支持回车发送
- 历史消息最多展示（`max-height: 300px`，超出滚动）

**追问类型判断**（v1.1 新增）：

| 用户意图 | 示例 | AI 行为 |
|---------|------|---------|
| 产品信息询问 | "有什么认证？"、"效率多少？"、"支持SNMP吗？" | 直接在分析区回答，推荐列表**不变** |
| 选型优化 | "换工频机型"、"需要并机5台"、"要求更大容量" | 重新分析，更新推荐列表 |

**追问时的产品数据补充**（v1.1 新增）：

`sendFollowup()` 在 system prompt 中额外附加当前推荐产品的完整参数，包括：

```
泰尔认证、CQC节能认证、满载效率、通讯接口、输入THDI、输出THDU、
并机卡、并机线、冷启动、电池启动、显示屏、备注
```

这些字段在首次选型的 `productSummary` 中被省略（为减少 token），但在追问场景中为 AI 提供完整数据，确保能回答认证、通讯协议等知识性问题。

**推荐列表保护**（v1.1 新增）：若 AI 回答知识性问题导致返回的推荐型号为空，自动沿用上一轮推荐结果，防止产品卡片消失。

---

## 四、技术架构

### 4.1 整体架构

```
单 HTML 文件
│
├── CSS（样式，内联）
├── HTML（结构，内联）
└── JavaScript（逻辑，内联）
    │
    ├── 数据层
    │   ├── PRODUCTS[]          ← 内置/导入的产品数据
    │   ├── IndexedDB           ← 导入的产品数据持久化（刷新不丢失）
    │   ├── localStorage        ← API配置、自定义提示词
    │   └── chatHistory[]       ← 多轮对话历史
    │
    ├── UI 层
    │   ├── 设置弹窗（双 Tab）
    │   ├── 数据库查看面板
    │   ├── 需求输入区
    │   ├── 结果展示区（双分区）
    │   ├── 对话追问区
    │   └── 产品详情弹窗
    │
    └── 服务层
        ├── callAI()            ← 统一 AI 调用（支持 OpenAI / Anthropic 格式）
        ├── analyze()           ← 首次选型
        ├── sendFollowup()      ← 多轮对话
        ├── importExcel()       ← Excel 导入
        ├── handleDocUpload()   ← 文档上传
        └── exportDoc()         ← 导出方案
```

### 4.2 关键函数索引

| 函数 | 行号（约） | 说明 |
|------|-----------|------|
| `callAI(userMsg, sysPrompt)` | ~303 | 统一 AI 调用，兼容 OpenAI 和 Anthropic 格式 |
| `analyze()` | ~408 | 首次选型主流程 |
| `sendFollowup()` | ~531 | 多轮追问 |
| `renderResult(result)` | ~477 | 渲染选型结果（双分区） |
| `buildProductRow(rec, idx)` | ~477 | 构建产品表格行 |
| `validateModels(result, valid)` | ~653 | 防幻觉：过滤不存在的型号 |
| `showDetail(model, reason)` | ~679 | 弹出产品详情 |
| `importExcel(input)` | ~710 | Excel 导入主函数 |
| `buildDataTable(rows)` | ~880 | 渲染双栏数据库表格 |
| `filterDataTable()` | ~940 | 数据库搜索过滤 |
| `dbSortSection(col, sec)` | ~985 | 数据库排序 |
| `exportDoc()` | ~718 | 导出 HTML 推荐方案 |
| `getCustomPrompt()` | ~347 | 读取自定义/默认提示词 |
| `openSettings()` / `switchSettingsTab()` | ~1105 | 设置弹窗管理 |
| `savePrompt()` / `resetPrompt()` | ~1140 | 提示词保存/重置 |
| `handleDocUpload(input)` | ~1155 | 文档上传解析 |

### 4.3 AI 调用数据流

```
用户输入需求文字
        ↓
analyze() / sendFollowup()
        ↓
构建 productSummary（从 PRODUCTS[] 提取关键字段）
        ↓
构建 systemPrompt = getCustomPrompt() + 产品数据库 + 合法型号清单 + 输出格式要求
        ↓
callAI(需求文字, systemPrompt)
        ↓
        ├── Anthropic API → POST /messages
        └── OpenAI 兼容 API → POST /chat/completions
        ↓
解析 JSON 响应 { analysis, recommended[], also_consider[] }
        ↓
validateModels() 过滤幻觉型号
        ↓
renderResult() 渲染到页面
```

### 4.4 Excel 导入数据流

```
用户选择 .xlsx/.xls 文件
        ↓
importExcel(input)
        ↓
SheetJS 读取所有 Sheet
        ↓
识别标准 UPS 系列（UR/UE/UM/UT）→ base + spec 两表 JOIN
        ↓
识别非标准 Sheet → 自动检测表头行和列名映射
        ↓
PRODUCTS[] 替换 → 显示 Toast → 刷新数据库视图
```

### 4.5 外部依赖（CDN）

| 库 | 版本 | 用途 |
|----|------|------|
| SheetJS | 0.20.3 | Excel 文件读取解析 |
| mammoth.js | 1.6.0 | DOCX 文档文本提取 |
| PDF.js | 3.11.174 | PDF 文档文本提取 |

> **注意**：以上库均从 CDN 加载，需要网络连接。若无网络，Excel 导入和文档上传功能不可用（已有提示），但内置数据选型不受影响。

---

## 五、数据格式规范

### 5.1 PRODUCTS 数组格式

每个产品对象包含以下字段（来自 Excel 合并后）：

```javascript
{
  "型号": "UE-2000TPL",
  "系列": "昆仑UE",
  "描述": "昆仑Power系列200kVA三进三出长机",
  "安装": "塔式",
  "并机数量": "4",
  "功率因数": "0.9",
  "满载效率": "0.9639",
  "输入电压范围(V)": "三相: 285~476",
  "通讯": "RS485/Modbus",
  // 泰尔技术规格字段（来自 *泰尔 Sheet）
  "额定输入电压": "380VAC",
  "输入电压范围": "304~456V",
  "输入频率范围": "50±2Hz",
  "输出电压稳定度": "≤0.2%",
  "输出THDU": "≤2%",
  // ... 更多技术参数
}
```

### 5.2 AI 响应 JSON 格式

```json
{
  "analysis": "■ 客户关键需求提取：\n容量: 200kVA...\n\n■ 容量选型分析：\n...\n\n■ 产品匹配逻辑：\n...\n\n■ 注意事项：\n...",
  "recommended": [
    {
      "model": "UE-2000TPL",
      "reason": "不少于80字的具体说明，逐项对应客户需求..."
    }
  ],
  "also_consider": [
    {
      "model": "UT-2000TPL",
      "reason": "备选原因..."
    }
  ]
}
```

### 5.3 localStorage 存储结构

```javascript
// API 配置
localStorage["ups_config"] = JSON.stringify({
  provider: "https://api.deepseek.com/v1",  // 或 "custom"
  model: "deepseek-chat",
  apikey: "sk-...",
  baseurl: "https://api.deepseek.com/v1",
  customProviderName: "硅基流动",           // 自定义提供商名称（provider="custom"时有效）
  customBaseurl: "https://api.siliconflow.cn/v1" // 自定义 Base URL
})

// 自定义提示词（不设置则用 DEFAULT_SYSTEM_PROMPT）
localStorage["ups_custom_prompt"] = "你是专业UPS选型工程师..."
```

---

## 六、产品内置数据说明

### 6.1 内置数据来源

数据来源：`常用UPS速查表-V7.5.xlsx`
内置时间：2026-03-04
共 **179 款产品**，4 个系列：

| 系列 | 品牌名 | 数量 |
|------|--------|------|
| UR 系列 | 太行 | 21 款 |
| UE 系列 | 昆仑 | 39 款 |
| UM 系列 | 祁连 | 22 款 |
| UT 系列 | 泰山 | 97 款 |

### 6.2 数据更新方法

**方法一（推荐）：导入新 Excel**
1. 更新 `常用UPS速查表-V7.5.xlsx`（或新版本）
2. 打开工具，点击"📂 导入 Excel"
3. 选择更新后的 Excel 文件
4. 数据立即生效，并自动保存到 IndexedDB
5. 刷新页面后数据自动恢复（显示"外部数据 xxx.xlsx | 导入于..."）
6. 如需恢复内置数据，点击"恢复内置数据"按钮

> 注意：导入的数据通过 IndexedDB 持久化存储，刷新页面后自动恢复，无需重复导入。如需永久更新内置数据，需重新执行内置数据嵌入（见 §8.2）。

**方法二（永久更新）：重新生成 HTML**
- 使用开发脚本将新 Excel 数据重新嵌入 HTML（需要开发人员操作）

---

## 七、安全说明

### 7.1 API Key 安全

- API Key 存储在用户本地浏览器（localStorage），不上传任何服务器
- 工具本身无后端，所有请求直接从用户浏览器发往 AI API
- Anthropic API 调用需要 `anthropic-dangerous-direct-browser-access: true` 头（绕过 CORS 限制）

### 7.2 数据安全

- 产品数据存储在 HTML 文件内，不上传
- 客户需求文字会发送给 AI API（用户需知晓）
- 不收集任何用户数据

---

## 八、开发与维护指南

### 8.1 本地开发环境

无需安装任何工具，只需：
- 现代浏览器（Chrome/Edge/Firefox）
- 文本编辑器（VS Code 推荐）
- Python 3.x（用于运行验证脚本）

### 8.2 修改核心提示词

**方式一（用户自助）**：打开工具 → ⚙️ 设置 → AI提示词 Tab → 修改 → 保存

**方式二（永久修改默认值）**：编辑 HTML 文件中的 `DEFAULT_SYSTEM_PROMPT` 常量（约第 280 行）

### 8.3 新增 API 提供商

**方式一（用户自助，无需修改代码）**：打开工具 → ⚙️ 设置 → API配置 → 下拉选"✏️ 自定义..." → 填写名称和 Base URL → 保存

**方式二（代码内置新预设）**：在 HTML 的 `PROVIDER_MODELS` 对象中添加映射，并在设置弹窗的 `<select id="provider">` 中添加 `<option>`：

```javascript
const PROVIDER_MODELS = {
  "https://api.openai.com/v1":     "gpt-4o",
  "https://api.deepseek.com/v1":   "deepseek-chat",
  // 添加新的提供商：
  "https://api.siliconflow.cn/v1": "Qwen/Qwen2.5-72B-Instruct",
};
```

### 8.4 修改产品表格显示列

编辑 `buildProductRow()` 函数（约第 477 行），增减 `tr.innerHTML` 中的列即可。
同时需要同步修改 HTML 中两个 `<thead>` 的列定义（`#product-table-rec` 和 `#product-table-alt`）。

### 8.5 验证脚本使用

```bash
# 验证提示词功能
python check_prompt_feature.py

# 验证双表数据库视图
python verify.py

# 检查 HTML 基础结构
python check_html.py

# 检查 Excel 数据结构
python inspect_excel.py
```

### 8.6 常见问题排查

| 问题 | 可能原因 | 解决方法 |
|------|---------|---------|
| 按钮点击无反应 | JS 语法错误，脚本未执行 | 打开浏览器 DevTools（F12）→ Console，查看报错 |
| 选型结果全是空 | AI 返回格式不符合 JSON | Console 查看 AI 原始返回，检查提示词 JSON 要求部分 |
| 推荐型号不存在 | 幻觉未被过滤（极少见） | 检查 validateModels 函数是否正常执行 |
| Excel 导入后数据少 | Sheet 识别规则不匹配 | 检查 Sheet 名和列名，或手动映射 |
| 刷新后导入数据丢失 | IndexedDB 加载失败 | F12 Console 查看是否有"加载持久化数据失败"错误；检查 `DOMContentLoaded` 初始化逻辑 |
| DOCX 无法解析 | mammoth.js CDN 未加载 | 检查网络，或切换 CDN |
| 测试连接失败 | CORS 问题或 Key 错误 | 浏览器直连 API 有 CORS 限制，需 API 支持 |

---

## 九、已知限制与待优化项

### 9.1 当前限制

| 限制 | 说明 |
|------|------|
| 无用户权限管理 | 无账号体系，不适合对外开放 |
| 产品数量限制 | 当产品数超过 500+ 时，Prompt 长度可能超出模型上下文限制 |
| 单语言 | 仅支持中文界面和中文需求 |

### 9.2 未来优化建议（优先级排序）

**P0 - 高优先级**
- [x] ~~导入数据持久化~~：Excel 导入后数据存入 IndexedDB，刷新不丢失（已完成）
- [x] ~~PDF 支持~~：集成 PDF.js 实现 PDF 文本提取（已完成）
- [x] ~~选型结果对比~~：支持多个产品并排对比参数差异，黄色高亮标注差异参数（已完成）

**P1 - 中优先级**
- [ ] **批量需求处理**：支持同时处理多个客户需求
- [ ] **选型历史记录**：保存每次选型的输入和结果
- [ ] **产品收藏**：标记常用或客户已确认的产品
- [ ] **自定义导出模板**：导出方案支持自定义封面、公司 Logo

**P2 - 低优先级**
- [ ] **多语言**：英文界面支持
- [ ] **移动端适配**：响应式布局优化
- [ ] **离线 AI**：集成本地小模型（Ollama）降低成本
- [ ] **版本管理**：多版本 Excel 数据源管理

---

## 十、线上部署

### 10.1 部署信息

| 项目 | 说明 |
|------|------|
| 平台 | Netlify（免费计划） |
| 线上地址 | https://ups-selector.netlify.app |
| Netlify 账号 | tianshangrenjian1989@gmail.com |
| Site ID | c662b019-245f-4a7f-af88-53c9b72502da |
| 部署目录 | `deploy/` |
| 访问密码 | 见内部记录 |

### 10.2 密码保护

首次访问需输入密码，验证通过后存入 `sessionStorage`，同一浏览器会话内刷新无需重新输入，关闭浏览器后需重新验证。

密码在 `UPS选型助手.html` 的 `checkAuth()` 函数中维护（前端验证，适合内部使用）。

### 10.3 更新部署命令

**方法一：使用 Netlify CLI（推荐）**

```bash
# 1. 安装 Netlify CLI（首次需要）
npm install -g netlify-cli

# 2. 登录（会打开浏览器）
netlify login

# 3. 部署（每次修改后执行）
cd "UPS选型助手_开发包_v1.2"
cp UPS选型助手.html deploy/index.html
cd deploy
netlify deploy --dir . --prod --site c662b019-245f-4a7f-af88-53c9b72502da
```

**方法二：手动上传**

1. 打开 https://app.netlify.com/sites/ups-selector
2. 将 `UPS选型助手.html` 拖拽到页面
3. 设置为生产环境

**方法三：使用 npx（无需全局安装）**

```bash
cd "UPS选型助手_开发包_v1.2"
cp UPS选型助手.html deploy/index.html
cd deploy
npx netlify deploy --dir . --prod --site c662b019-245f-4a7f-af88-53c9b72502da
```

> 注意：首次使用 Netlify CLI 需要登录认证。

---

## 十、版本历史

| 版本 | 日期 | 主要变更 |
|------|------|---------|
| v0.1 | 2026-03-04 | 初始版本：内置数据 + 基础 AI 选型 |
| v0.2 | 2026-03-04 | 修复 JS 语法错误（全角引号 Bug） |
| v0.3 | 2026-03-04 | 新增多轮对话追问功能 |
| v0.4 | 2026-03-04 | 新增 Excel 导入 + 数据库查看器 |
| v0.5 | 2026-03-04 | 优化 Excel 导入：支持多种 Sheet 格式自动识别 |
| v0.6 | 2026-03-04 | 上下双栏数据库视图（产品信息 + 泰尔参数） |
| v0.7 | 2026-03-04 | API 配置移入设置弹窗 + 文档上传功能 |
| v0.8 | 2026-03-04 | 修复 IIFE DOM 访问顺序 Bug（查看数据库不显示） |
| v1.0 | 2026-03-04 | 专业提示词系统（6维度分析 + 自定义） + 结果展示拆分（推荐/备选独立区块）|
| v1.1 | 2026-03-04 | 多轮对话增强（知识性问题直接作答 + 追问附加完整产品参数 + 推荐列表保护）；API提供商支持自定义名称和URL |
| v1.2 | 2026-03-09 | 新增电池配置计算功能（铅酸电池/锂电池/开关盒）；支持负载功率和UPS功率两种输入模式；导入数据持久化（IndexedDB）；修复 IndexedDB 初始化顺序 Bug（IIFE 中调用未定义函数，改用 DOMContentLoaded）；工程化改进（CSS分离、构建脚本、测试入口） |
| v1.2.1 | 2026-03-10 | 新增 PDF 文档上传支持（集成 PDF.js 3.11.174）；文档上传现在支持 TXT/MD/DOCX/PDF 格式 |
| v1.2.2 | 2026-03-10 | 部署至 Netlify（https://ups-selector.netlify.app）；新增前端密码保护遮罩（sessionStorage 保持登录状态） |
| v1.2.3 | 2026-03-10 | 新增选型结果对比功能：推荐/备选产品支持复选框选择，2-4产品并排对比，黄色高亮标注差异参数 |

---

## 十一、附录

### A. 支持的产品系列说明

| 系列代码 | 品牌名 | 特点 |
|---------|--------|------|
| UR | 太行 | 中小功率，工频机型为主 |
| UE | 昆仑 | 中大功率，三进三出，含长机/标机 |
| UM | 祁连 | 模块化机型，支持热插拔扩容 |
| UT | 泰山 | 全系列覆盖，含单进单出/三进三出，型号最多 |

### B. 关键术语

| 术语 | 说明 |
|------|------|
| 标机 | 标准后备时间机型（含内置电池） |
| 长机 | 长后备时间机型（外接电池组） |
| 工频机 | 基于工频变压器，抗冲击能力强，效率较低 |
| 高频机 | 基于 IGBT 高频变换，效率高，体积小 |
| 单进单出 | 单相输入、单相输出（1:1） |
| 三进三出 | 三相输入、三相输出（3:3） |
| 并机 | 多台 UPS 并联运行，提高容量和冗余 |
| 泰尔 | 中国信息通信研究院（CATR）认证，数据中心领域权威认证 |
| THDI | 输入电流总谐波失真率，越低越好 |
| THDU | 输出电压总谐波失真率，越低越好 |
| PF | 功率因数（Power Factor） |
| ECO | 节能运行模式 |
| MTBF | 平均无故障时间 |
| MTTR | 平均修复时间 |

---

*文档由 Claude Code 自动生成，如有更新请同步维护本文档。*
