# 已修复的Bug与开发注意事项

（已更新：2026-03-17）

## 已修复Bug

### 1. IndexedDB 持久化初始化顺序Bug（2026-03-09）
- **症状**: 刷新页面后导入数据丢失，控制台报错 `Cannot access 'db' before initialization`
- **原因**: `loadProductsFromDB()` 在IIFE中被调用时函数尚未定义
- **修复**: 改用 `DOMContentLoaded` 延迟调用
- **关键**: IndexedDB相关函数必须在DOMContentLoaded回调之前定义

### 2. 开关盒计算JS语法错误（2026-03-12）
- **症状**: 按钮点击无反应
- **原因**: 修改代码时出现语法错误（缺少括号、变量名拼写等）
- **修复**: 仔细检查代码语法，每次修改后浏览器测试

### 3. Netlify部署配置错误（2026-03-12）
- **症状**: Netlify报错 "deploy directory is not a directory"
- **原因**: `netlify.toml` 中 `publish` 配置为文件而非目录
- **修复**: 改为 `publish = "."`，添加重定向规则

### 4. 铅酸电池数据不完整（v1.3.0, 2026-03-14）
- **症状**: 缺少2V电池，放电时间只有部分
- **修复**: 完整遍历所有时间列，正确识别所有电池型号

### 5. 下拉框选项与数据不匹配（v1.3.0）
- **修复**: 根据实际数据动态生成下拉框选项

### 6. GitHub Pages 未更新（v1.3.0）
- **原因**: index.html未同步
- **修复**: 每次推送后同步更新index.html

### 7. HTML标签语法错误（v1.3.0）
- **原因**: `<\label>` 错误闭合标签
- **修复**: 检查HTML标签语法

## 电池监控配置模块 Bug（v1.3.2, 2026-03-16）

### Bug 8-15 摘要
- **表格列不全**: HTML th列数与渲染函数td列数不一致 → 添加产品描述列
- **锂电池选项**: Excel中无锂电池数据 → 删除锂电池选项
- **单体测量模块数量**: 应为每只电池配1个，非每24只配1个
- **电流监控模块遗漏**: 所有类型电池都添加1个电流监控模块
- **大括号不匹配**: 用Node.js语法检查定位
- **后备时间字段**: 用户要求删除
- **放电截止电压**: 改为下拉框（1.60V~1.85V）
- **分组电流公式**: 组数=1时分组电流=最大电流，>1时=最大电流/(组数-1)

## v1.3.1 优化（2026-03-14）
- 电池类型下拉框替换单格个数输入（12V/2V电池）
- 根据电池类型动态显示对应推荐Tab

## v1.3.4 代码优化（2026-03-17）
- 格式化5个压缩单行函数为正常缩进格式
- 提取 `buildProductSummary()` 公共函数，消除重复代码
- 删除未使用的 `getLithiumRecommendations` 死代码

## v1.3.5 功能与修复（2026-03-17）
- 电池监控配置表导出改为Excel格式(.xlsx)
- 引入xlsx-js-style替换SheetJS，支持单元格样式（边框、背景色、字体）
- 导出根据价格解锁状态决定是否包含价格列
- 修复2V电池最大电流计算错误：2V电池cellMultiplier=1，12V电池=6

## v1.3.6 修复（2026-03-19）

### Bug 16: 电池监控主机/显示屏数量计算错误
- **症状**: 固定显示1个，与Excel计算不一致
- **Excel公式**: `=ROUNDUP(C6/6,0)` (电池总组数/6向上取整)
- **修复**: 主机数量 = Math.ceil(totalBattGroups / 6)，显示屏数量同主机

### Bug 17: 蓄电池组电流监控/电流传感器数量错误
- **症状**: 固定显示1个
- **Excel公式**: `=C6` (电池总组数)
- **修复**: 数量 = totalBattGroups

### Bug 18: 电压采集模块数量错误
- **症状**: 固定显示1个
- **Excel公式**: `=IF(H4="是",C6,0)` (选配时=电池总组数)
- **修复**: 数量 = totalBattGroups

### Bug 19: 分组放电电流公式错误
- **错误公式**: `maxCurrent / (totalBattGroups - 1)`
- **正确公式**: `maxCurrent / totalBattGroups`
- **修复**: 分组电流 = 最大电流 / 电池组数

### Bug 20: 产品描述与Excel不一致
- **症状**: 自行简化的描述与Excel原始数据不符
- **修复**: 严格从Excel读取完整原始描述

### Bug 21: 表格描述列显示不全
- **原因**: 固定列宽+文字截断
- **修复**: 设置 min-width + word-break:break-word

## 核心教训：数据源优先原则

### 犯错的根本原因
1. **未读取数据源**: 用户提到"Excel里有公式"时，应立即读取Excel
2. **自作主张**: 自行简化描述，导致与源数据不一致
3. **样式理解不深**: 对table换行机制理解不足

### 改进方法
- 用户提供参考文件时，第一步是读取/查看参考内容
- 严格保持与源文件数据一致，不自行简化
- 修改后对照原始数据验证一遍

## 开发注意事项
1. **函数定义顺序**: 初始化阶段调用的函数必须定义在调用位置之前，或用DOMContentLoaded延迟
2. **file://协议**: 双击HTML使用，IndexedDB可用但有限制
3. **单文件约束**: 所有代码内联，修改时注意不要破坏script标签结构
4. **同步index.html**: 每次修改后必须同步
5. **浏览器测试**: 功能完成后用浏览器验证
6. **语法检查**: 用Node.js `new Function()` 检查JS语法
7. **部署优先**: GitHub Pages（免费无限）> Netlify（有限额）
8. **自我验证清单**: HTML表格列数=渲染函数列数、输入字段=处理变量、Excel数据=代码数据
